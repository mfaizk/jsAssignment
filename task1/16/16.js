const PI = 3.14;

var radius = prompt("Enter Radius");

var area = PI * r * r;
var circum = 2 * PI * r;

alert(`{Area of circle is ${area} and circumference is ${circum}}`);

console.log(area);
console.log(circum);
